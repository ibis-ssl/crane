diff --git a/crane_bringup/launch/crane.launch.xml b/crane_bringup/launch/crane.launch.xml
index 5983039b..40d0548e 100644
--- a/crane_bringup/launch/crane.launch.xml
+++ b/crane_bringup/launch/crane.launch.xml
@@ -38,8 +38,8 @@ limitations under the License.

   <!-- sim=falseの場合（公式ポート） -->
   <let name="vision_port_final" value="10006" if="$(eval &quot;'$(var vision_port)' == '' and '$(var sim)' == 'false'&quot;)"/>
-  <let name="referee_port_final" value="10003" if="$(eval &quot;'$(var referee_port)' == '' and '$(var sim)' == 'false'&quot;)"/>
-  <let name="tracker_port_final" value="10010" if="$(eval &quot;'$(var tracker_port)' == '' and '$(var sim)' == 'false'&quot;)"/>
+  <let name="referee_port_final" value="11003" if="$(eval &quot;'$(var referee_port)' == '' and '$(var sim)' == 'false'&quot;)"/>
+  <let name="tracker_port_final" value="11010" if="$(eval &quot;'$(var tracker_port)' == '' and '$(var sim)' == 'false'&quot;)"/>

   <!-- ユーザー指定がある場合はそれを使用 -->
   <let name="vision_port_final" value="$(var vision_port)" unless="$(eval &quot;'$(var vision_port)' == ''&quot;)"/>
@@ -57,7 +57,7 @@ limitations under the License.
   <!-- ============================================================ -->
   <!-- ロボット性能パラメータ -->
   <!-- ============================================================ -->
-  <arg name="max_vel" default="8.0" description="ロボットの最大速度 [m/s]"/>
+  <arg name="max_vel" default="3.0" description="ロボットの最大速度 [m/s]"/>

   <!-- Slack時間計算パラメータ -->
   <arg name="slack.robot_max_acceleration" default="2.5" description="slack時間計算に用いるロボットの最大加速度 [m/s^2]"/>
@@ -72,14 +72,14 @@ limitations under the License.
   <!-- 加減速度パラメータ -->
   <!-- ============================================================ -->
   <!-- ロボット送信用の加速度パラメータ -->
-  <arg name="robot_acceleration.acceleration" default="7.0" description="加速時の加速度 [m/s^2]"/>
-  <arg name="robot_acceleration.deceleration_high" default="5.0" description="減速時・高速域の減速度 [m/s^2]"/>
-  <arg name="robot_acceleration.deceleration_low" default="2.0" description="減速時・低速域の減速度 [m/s^2]"/>
+  <arg name="robot_acceleration.acceleration" default="3.0" description="加速時の加速度 [m/s^2]"/>
+  <arg name="robot_acceleration.deceleration_high" default="3.0" description="減速時・高速域の減速度 [m/s^2]"/>
+  <arg name="robot_acceleration.deceleration_low" default="3.0" description="減速時・低速域の減速度 [m/s^2]"/>
   <arg name="robot_acceleration.velocity_threshold" default="1.0" description="高速域・低速域のしきい値 [m/s]"/>

   <!-- 経路計画用の減速度パラメータ -->
-  <arg name="planning_deceleration.high_speed" default="5.0" description="高速域での減速計算用の減速度 [m/s^2]"/>
-  <arg name="planning_deceleration.low_speed" default="2.5" description="低速域での減速計算用の減速度 [m/s^2]"/>
+  <arg name="planning_deceleration.high_speed" default="3.0" description="高速域での減速計算用の減速度 [m/s^2]"/>
+  <arg name="planning_deceleration.low_speed" default="3.0" description="低速域での減速計算用の減速度 [m/s^2]"/>
   <arg name="planning_deceleration.velocity_threshold" default="1.0" description="高速域・低速域のしきい値 [m/s]"/>

   <!-- ============================================================ -->
diff --git a/crane_commentary/crane_commentary/self_commentary/intent_tracker.py b/crane_commentary/crane_commentary/self_commentary/intent_tracker.py
index d0b8e5b5..6619ffa2 100644
--- a/crane_commentary/crane_commentary/self_commentary/intent_tracker.py
+++ b/crane_commentary/crane_commentary/self_commentary/intent_tracker.py
@@ -104,7 +104,7 @@ class IntentTracker:

         # Build new mapping
         for result in msg.results:
-            tactic_name = result.name
+            tactic_name = result.tactic_name
             for robot_id in result.selected_robots:
                 self._robot_to_tactic[robot_id] = tactic_name

diff --git a/crane_local_planner/src/rvo2_planner.cpp b/crane_local_planner/src/rvo2_planner.cpp
index d3bb7d8a..b09a1b8f 100644
--- a/crane_local_planner/src/rvo2_planner.cpp
+++ b/crane_local_planner/src/rvo2_planner.cpp
@@ -98,6 +98,7 @@ auto RVO2Planner::reflectWorldToRVOSim(crane_msgs::msg::PositionCommands & msg)
         return Point(command.current_pose.x, command.current_pose.y);
       }
     }();
+    current_position << command.current_pose.x, command.current_pose.y;

     // 位置指令の処理
     Vector2 position_diff;
@@ -157,11 +158,12 @@ auto RVO2Planner::reflectWorldToRVOSim(crane_msgs::msg::PositionCommands & msg)
     trajectory.generate(
       Eigen::Vector2d(current_position.x(), current_position.y()),
       Eigen::Vector2d(command.target_x, command.target_y),
-      Eigen::Vector2d(command.current_velocity.x, command.current_velocity.y), max_vel, max_acc);
+      Eigen::Vector2d(command.current_velocity.x, command.current_velocity.y) * 0.0, max_vel,
+      max_acc);

     // BangBangTrajectoryから速度を取得
     // Vision遅延（~100ms）を考慮してlookahead時間を設定
-    const double lookahead_time = 0.1;
+    const double lookahead_time = 0.3;
     Eigen::Vector2d next_vel = trajectory.getVelocity(lookahead_time);

     // 目標との距離を計算
@@ -172,7 +174,7 @@ auto RVO2Planner::reflectWorldToRVOSim(crane_msgs::msg::PositionCommands & msg)
     // terminal_velocity（0の場合はフォールバック値0.3 m/sを使用）
     const double terminal_vel = command.local_planner_config.terminal_velocity > 0
                                   ? command.local_planner_config.terminal_velocity
-                                  : 0.3;
+                                  : (0.1 + distance_to_target);
     const double min_distance =
       std::max(static_cast<double>(command.position_tolerance) * 2, 0.03);  // 最低3cm

diff --git a/crane_msgs/msg/control/RobotFeedback.msg b/crane_msgs/msg/control/RobotFeedback.msg
index 205b329f..a55e768b 100755
--- a/crane_msgs/msg/control/RobotFeedback.msg
+++ b/crane_msgs/msg/control/RobotFeedback.msg
@@ -1,5 +1,6 @@
 builtin_interfaces/Time received_stamp
 uint8 robot_id
+float32 packet_frequency_hz

 uint8 counter

diff --git a/crane_robot_receiver/src/robot_receiver_node.cpp b/crane_robot_receiver/src/robot_receiver_node.cpp
index 5ad5cb65..a22ec8c7 100644
--- a/crane_robot_receiver/src/robot_receiver_node.cpp
+++ b/crane_robot_receiver/src/robot_receiver_node.cpp
@@ -11,6 +11,7 @@
 #include <crane_msgs/msg/robot_feedback.hpp>
 #include <crane_msgs/msg/robot_feedback_array.hpp>
 #include <crane_robot_receiver/robot_feedback_protocol.hpp>
+#include <deque>
 #include <format>
 #include <rclcpp/rclcpp.hpp>
 #include <unordered_set>
@@ -36,6 +37,8 @@ public:
   {
     if (receiver_->available()) {
       receiver_->receive(buffer_);
+      // 受信タイムスタンプを記録
+      receive_timestamps_.push_back(clock.now());
       return true;
     }
     return false;
@@ -117,6 +120,21 @@ public:

   auto getFeedback() const -> RobotFeedback { return robot_feedback; }

+  auto getPacketFrequency() -> float
+  {
+    using std::chrono::operator""ms;
+    auto now = clock.now();
+
+    // 1秒より古いタイムスタンプを削除
+    while (!receive_timestamps_.empty() &&
+           (now - receive_timestamps_.front()) > 1000ms) {
+      receive_timestamps_.pop_front();
+    }
+
+    // 最近1秒間の受信回数を返す
+    return static_cast<float>(receive_timestamps_.size());
+  }
+
   const int robot_id;

 private:
@@ -127,6 +145,9 @@ private:
   RobotFeedback robot_feedback;

   rclcpp::Clock clock;
+
+  // パケット受信頻度計算用のタイムスタンプキュー
+  std::deque<rclcpp::Time> receive_timestamps_;
 };

 class RobotReceiverNode : public rclcpp::Node
@@ -168,6 +189,7 @@ public:
           crane_msgs::msg::RobotFeedback robot_feedback_msg;
           robot_feedback_msg.received_stamp = robot_feedback.received_stamp;
           robot_feedback_msg.robot_id = receiver->robot_id;
+          robot_feedback_msg.packet_frequency_hz = receiver->getPacketFrequency();
           robot_feedback_msg.counter = robot_feedback.counter;
           robot_feedback_msg.kick_state = robot_feedback.kick_state;
           for (auto temperature : robot_feedback.temperature) {
diff --git a/crane_robot_skills/src/kick.cpp b/crane_robot_skills/src/kick.cpp
index ec34acdd..ae863f1a 100644
--- a/crane_robot_skills/src/kick.cpp
+++ b/crane_robot_skills/src/kick.cpp
@@ -105,7 +105,9 @@ void Kick::initialize()

   addStateFunction(KickState::AROUND_BALL_AND_KICK, [this]() {
     auto target = getParameter<Point>("target");
-    Point ball_pos = world_model()->ball().pos;
+    double ball_dist = (robot()->pose.pos - world_model()->ball().pos).norm();
+    Point ball_pos =
+      world_model()->ball().getPredictedPosition(std::clamp(ball_dist - 0.5, 0.0, 3.0));
     // 視認性の高いキック方向の可視化: 太い矢印 + 角度しきい値の扇
     {
       Vector2 dir = (target - ball_pos).normalized();
@@ -140,7 +142,7 @@ void Kick::initialize()
     Vector2 kick_vec = (target - ball_pos).normalized();
     double kick_vec_gain = [&]() {
       Segment ball_kick_zone{ball_pos, ball_pos - kick_vec * INTERVAL};
-      if (bg::distance(ball_kick_zone, robot()->pose.pos) < 0.1) {
+      if (bg::distance(ball_kick_zone, robot()->pose.pos) < 0.15) {
         command->disableCollisionAvoidance();
         return 0.5;
       } else {
diff --git a/crane_tactics/config/test_planner.yaml b/crane_tactics/config/test_planner.yaml
index 94e62f89..95769655 100644
--- a/crane_tactics/config/test_planner.yaml
+++ b/crane_tactics/config/test_planner.yaml
@@ -2,9 +2,9 @@

 # 経由点で未指定の場合に適用する既定値
 defaults:
-  max_velocity: 3.0        # m/s
-  max_acceleration: 2.5    # m/s^2
-  sleep_sec: 5.0           # 各経由点での休憩秒数（省略時）
+  max_velocity: 6.0        # m/s
+  max_acceleration: 4.5    # m/s^2
+  sleep_sec: 1.0           # 各経由点での休憩秒数（省略時）

 waypoints:
   # position: [x, y] または [x, y, theta(度)]
@@ -16,7 +16,7 @@ waypoints:
   # - position: [-3.5, -2.0, -90]
   # - position: [-3.5, 2.0, 0]
   # - position: [0.0, 0.0]    # 角度なし（位置のみ）
-  - position: [4.5, 2.5]
-  - position: [-4.5, 2.5]
+  - position: [-2.5, 2.5]
+  - position: [-2.5, -2.5]
 # このプランナで動かす自チームロボットID
-robot_id: 0
+robot_id: 10
diff --git a/docker/config/engine.yaml b/docker/config/engine.yaml
index 3db1fe92..b9a66ea6 100644
--- a/docker/config/engine.yaml
+++ b/docker/config/engine.yaml
@@ -91,54 +91,54 @@
       }
     }
   },
-  "activeTrackerSource":  "5901a9cc-4684-4f34-88c5-9640a971d52c",
+  "activeTrackerSource":  "96fc5817-5143-4df6-bb2a-a55676cd5184",
   "teams":  [
-    "MIT Roboteam",
-    "Sysmic Robotics",
-    "Unknown",
-    "luhbots",
-    "MRL",
-    "OrcaBOT",
-    "ULtron",
-    "Ri-One",
     "A-Team",
-    "ITAndroids",
-    "MCT Susano Logics",
-    "NEUIslanders",
-    "Test Team",
-    "Tritons RCSC",
-    "AMC",
-    "OP-AmP",
-    "URoboRus",
-    "Warthog Robotics",
-    "ZJUNlict",
+    "Immortals",
+    "KIKS",
+    "Sysmic Robotics",
     "nAMeC",
-    "NAELIC",
+    "Ri-One",
+    "CMμs",
+    "Parsian",
     "RoboDragons",
+    "RoboJackets",
+    "STOx’s",
+    "UBC Thunderbots",
+    "Warthog Robotics",
     "SRC",
+    "AIS",
+    "ER-Force",
+    "MIT Roboteam",
+    "OrcaBOT",
+    "RobôCin",
+    "RoboFEI",
+    "RoboTeam Twente",
+    "Test Team",
+    "NEUIslanders",
+    "OMID",
+    "Ri-one",
+    "RoboIME",
+    "Roots",
+    "MCT Susano Logics",
+    "MRL",
+    "RFC Cambridge",
     "SSH",
     "TIGERs Mannheim",
+    "Unknown",
+    "AMC",
+    "OP-AmP",
+    "Tritons RCSC",
+    "ULtron",
     "UMass Minutebots",
-    "Immortals",
+    "ZJUNlict",
+    "ibis",
+    "ITAndroids",
     "KgpKubs",
-    "Parsian",
-    "RFC Cambridge",
-    "RoboTeam Twente",
-    "CMμs",
-    "ER-Force",
-    "KIKS",
+    "luhbots",
+    "NAELIC",
     "NAMeC",
-    "RobôCin",
-    "RoboIME",
-    "UBC Thunderbots",
-    "ibis",
-    "AIS",
-    "OMID",
-    "Ri-one",
-    "RoboFEI",
-    "RoboJackets",
-    "STOx’s",
-    "Roots"
+    "URoboRus"
   ],
-  "autoContinue":  true
+  "autoContinue":  false
 }
diff --git a/docker/real/ssl-vision-client-config.json b/docker/real/ssl-vision-client-config.json
index 4f8d62d7..a508ef27 100644
--- a/docker/real/ssl-vision-client-config.json
+++ b/docker/real/ssl-vision-client-config.json
@@ -1,5 +1,5 @@
 {
   "visionPort": 10006,
-  "trackedPort": 10010,
-  "refereePort": 10003
+  "trackedPort": 11010,
+  "refereePort": 11003
 }
diff --git a/docker/sim/ssl-vision-client-config.json b/docker/sim/ssl-vision-client-config.json
index 4d7f66a3..a508ef27 100644
--- a/docker/sim/ssl-vision-client-config.json
+++ b/docker/sim/ssl-vision-client-config.json
@@ -1,5 +1,5 @@
 {
-  "visionPort": 10020,
+  "visionPort": 10006,
   "trackedPort": 11010,
   "refereePort": 11003
 }
diff --git a/utility/crane_physics/include/crane_physics/bang_bang_trajectory.hpp b/utility/crane_physics/include/crane_physics/bang_bang_trajectory.hpp
index 4ac191df..6298619a 100644
--- a/utility/crane_physics/include/crane_physics/bang_bang_trajectory.hpp
+++ b/utility/crane_physics/include/crane_physics/bang_bang_trajectory.hpp
@@ -304,6 +304,7 @@ class BangBangTrajectory2D
 public:
   BangBangTrajectory1D x;
   BangBangTrajectory1D y;
+  double max_vel = 0.0;

   /// 二分探索の初期刻み幅
   static constexpr double BINARY_SEARCH_INITIAL_INCREMENT = M_PI / 8.0;
@@ -323,7 +324,12 @@ public:

   [[nodiscard]] Eigen::Vector2d getVelocity(const double t) const noexcept
   {
-    return Eigen::Vector2d(x.getVelocity(t), y.getVelocity(t));
+    Eigen::Vector2d vel(x.getVelocity(t), y.getVelocity(t));
+    const double vel_norm_sq = vel.squaredNorm();
+    if (max_vel > 0.0 && vel_norm_sq > max_vel * max_vel) {
+      return vel * (max_vel / std::sqrt(vel_norm_sq));
+    }
+    return vel;
   }

   [[nodiscard]] Eigen::Vector2d getAcceleration(const double t) const noexcept
@@ -349,6 +355,7 @@ public:
     const Eigen::Vector2d & s0, const Eigen::Vector2d & s1, const Eigen::Vector2d & v0,
     const double vmax, const double acc, const double accuracy = DEFAULT_SYNC_ACCURACY)
   {
+    max_vel = vmax;
     const double s0x = s0.x();
     const double s0y = s0.y();
     const double s1x = s1.x();
