#!/usr/bin/env bash
# ワークスペースを source していなくても動く入口（robot_bench の ./bench 相当）。
# パッケージのあるディレクトリを PYTHONPATH に足して cli を呼ぶだけ。
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${DIR}${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m crane_packet_forge.cli "$@"
